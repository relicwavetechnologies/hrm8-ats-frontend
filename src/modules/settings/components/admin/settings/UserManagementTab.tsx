import { Card, CardContent, CardHeader, CardTitle } from "@/shared/components/ui/card";

export function UserManagementTab() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>User Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>User Management module is currently being ported.</p>
      </CardContent>
    </Card>
  );
}
