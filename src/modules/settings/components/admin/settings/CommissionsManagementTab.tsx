import { Card, CardContent, CardHeader, CardTitle } from "@/shared/components/ui/card";

export function CommissionsManagementTab() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Commissions Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Commissions Management module is currently being ported.</p>
      </CardContent>
    </Card>
  );
}
