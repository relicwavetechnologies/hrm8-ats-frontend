import { Card, CardContent, CardHeader, CardTitle } from "@/shared/components/ui/card";

export function AuditLogsTab() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Audit Logs</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Audit Logs module is currently being ported.</p>
      </CardContent>
    </Card>
  );
}
